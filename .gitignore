# Runtime data
dumped_tables/
preload_dumps/
debug/

# Upstream-Cache (auto-cloned by compare_offsets.py)
.upstream_cache/

# Python cache
__pycache__/
*.pyc

# IDE / Editor
.vscode/
.continue/

# Per-user Claude Code settings
.claude/settings.local.json

# Logs & temp files
logs/
*.error.log
*.error.log.*

# Runtime-generated (user-specific)
offset_history.json
last_known_patch.txt  # legacy — PatchChecker now stores this in the config INI
gamehelper_config.ini
hotkeys.json
hotkeys_export/
desktop.ini

# Dev notes (not for repo)
NEXT_STEPS.md
QUICKSTART.md
STAT_EXTRACTION_README.md

# Raw extraction output (regenerated from game files)
data/raw_csv/
data/raw_extracted/

# Local-only dev / test entrypoints (not part of the shipped helper)
dev/
